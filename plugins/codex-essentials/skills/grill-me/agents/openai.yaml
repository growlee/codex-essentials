interface:
  display_name: "Grill Me"
  short_description: "Interview one material decision at a time"
  default_prompt: "Use $grill-me to stress-test this plan through focused decision questions."
policy:
  allow_implicit_invocation: false
