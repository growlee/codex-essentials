interface:
  display_name: "Self Check"
  short_description: "Check repetition, drift, and reopened decisions"
  default_prompt: "Use $self-check to check this work once for repetition, drift, or reopened decisions."
policy:
  allow_implicit_invocation: false
